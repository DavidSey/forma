<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('home') }}">Communaute-Ebeniste.fr</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto ">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('home') }}">Accueil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('productions') }}">Réalisations</a>
            </li>
            <li class="nav-item">
                {% if is_granted('ROLE_USER') %}
                    <a class="nav-link" href="{{ path('production_new') }}">Ajouter une réalisation</a>
                {% endif %}
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('contact') }}">Contact</a>
            </li>
            <li class="nav-item">
                {% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    <a class="nav-link" href="{{ path('login') }}">Connexion</a>
                {% endif %}
            </li>
            <li class="nav-item">
                {% if is_granted('ROLE_USER') %}
                    <a class="nav-link" href="{{ path('logout_path') }}">Déconnexion</a>
                {% endif %}
            </li>
            <li  class="nav-item">
                {% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    <a class="nav-link"  href="{{ path('app_register') }}">Inscription</a>
                {% endif %}
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('csrf') }}">Faille</a>
            </li>

        </ul>
    </div>
</nav>